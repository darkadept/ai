import React, {Component} from 'react';
import Grid from './Grid';

class App extends Component {
  render() {
    return (
      <Grid
        width={20}
        height={20}
        pixelSize={30}
      >
      </Grid>
    );
  }
}

export default App;
